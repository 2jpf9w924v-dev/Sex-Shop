const bcrypt=require('bcryptjs');const {db}=require('./_db');const {makeToken,sessionCookie}=require('./_auth');module.exports=async(req,res)=>{if(req.method!=='POST')return res.status(405).json({error:'Método não permitido.'});try{const email=String(req.body?.email||'').trim().toLowerCase(),password=String(req.body?.password||'');const sql=db();const u=await sql`SELECT id,name,email,password_hash FROM admin_users WHERE email=${email} AND active=true LIMIT 1`;if(!u[0]||!(await bcrypt.compare(password,u[0].password_hash)))return res.status(401).json({error:'E-mail ou senha inválidos.'});res.setHeader('Set-Cookie',sessionCookie(makeToken(u[0])));return res.json({ok:true,name:u[0].name})}catch(e){return res.status(500).json({error:'Erro no login.',details:e.message})}};
