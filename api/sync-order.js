const {syncOrder}=require('./_sync-order');module.exports=async(req,res)=>{if(!['GET','POST'].includes(req.method))return res.status(405).json({error:'Método não permitido.'});try{const id=String(req.query?.order_id||req.body?.order_id||'');if(!id)return res.status(400).json({error:'order_id obrigatório.'});const r=await syncOrder(id);return res.json({ok:true,found:r.found,paid:r.paid,status:r.mp?.status,status_detail:r.mp?.status_detail,external_reference:r.mp?.external_reference,email:r.email})}catch(e){console.error(e);return res.status(500).json({error:'Falha ao sincronizar pedido.',details:e.message})}}
