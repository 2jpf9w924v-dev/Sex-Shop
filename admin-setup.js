const bcrypt=require('bcryptjs');const {db}=require('./_db');
module.exports=async(req,res)=>{res.setHeader('Content-Type','application/json; charset=utf-8');if(req.method==='GET'){try{const sql=db();const r=await sql`SELECT count(*)::int AS count FROM admin_users WHERE active=true`;return res.json({setup_required:r[0].count===0})}catch(e){return res.status(500).json({error:e.message})}}if(req.method!=='POST')return res.status(405).json({error:'Método não permitido.'});try{const sql=db();const c=await sql`SELECT count(*)::int AS count FROM admin_users`;if(c[0].count>0)return res.status(409).json({error:'Configuração inicial já concluída.'});const name=String(req.body?.name||'').trim(),email=String(req.body?.email||'').trim().toLowerCase(),password=String(req.body?.password||'');if(name.length<2||!/^[^@\s]+@[^@\s]+\.[^@\s]+$/.test(email)||password.length<10)return res.status(400).json({error:'Informe nome, e-mail válido e senha com pelo menos 10 caracteres.'});const hash=await bcrypt.hash(password,12);await sql`INSERT INTO admin_users(name,email,password_hash) VALUES(${name},${email},${hash})`;return res.status(201).json({ok:true})}catch(e){console.error(e);return res.status(500).json({error:'Erro ao criar administrador.',details:e.message})}};
